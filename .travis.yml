language: python
python:
  - "2.7"
  - "3.3"
  - "pypy"

install:
  - pip install -r requirements-ci.txt --use-mirrors
  - pip install . --use-mirrors

script: PYTHONPATH=tests python -munittest centidb_test keycoder_test

notifications:
  email: false
